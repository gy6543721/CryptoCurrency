name: iOS Test Workflow

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  run_tests:
    runs-on: macos-latest
    strategy:
      matrix:
        include:
          - xcode: "15.1"
            ios: "17.2"
          - xcode: "15.1"
            ios: "16.0"
    name: Test iOS (${{ matrix.ios }})
    steps:
    - uses: actions/checkout@v3
    - name: Select Xcode
      run: sudo xcode-select -switch /Applications/Xcode_${{ matrix.xcode }}.app && /usr/bin/xcodebuild -version
    - name: Run unit tests
      run: xcodebuild test -scheme CryptoCurrency -project CryptoCurrency.xcodeproj -destination 'platform=iOS Simulator,name=iPhone 15,OS=${{ matrix.xcode }}' | xcpretty && exit ${PIPESTATUS[0]}