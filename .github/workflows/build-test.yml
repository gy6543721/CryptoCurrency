name: Build & Test

on:
  push:
    branches:
      - master
  pull_request:
  
jobs:

  build:
  
    runs-on: macOS-latest
    
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v3
    - name: Build & Test
      run: xcodebuild test -scheme CryptoCurrency -destination 'platform=iOS Simulator,OS=latest,name=iPhone 15'